
Для Kircraft в первой версии интеграции логично считать базовыми такие поля YooKassa:
	•	id
	•	status
	•	amount.value
	•	amount.currency
	•	description
	•	created_at
	•	paid
	•	refundable
	•	test
	•	metadata
	•	confirmation.confirmation_url
	•	payment_method.type
	•	captured_at
	•	expires_at
	•	cancellation_details

  Что сохранять в БД после создания платежа минимум
  Минимально:
	•	внутренний id платежа
	•	order_id
	•	yookassa_payment_id
	•	status
	•	amount_value
	•	amount_currency
	•	description
	•	confirmation_url
	•	paid
	•	refundable
	•	test
	•	created_at
	•	raw_response


# YooKassa Payment Integration

## 1. Общее описание интеграции

Интеграция с YooKassa используется в системе Kircraft для обработки онлайн-платежей клиентов при оформлении заказа через клиентские интерфейсы системы.

Клиентские интерфейсы системы могут включать:

- Telegram-бот
- Web-интерфейс
- Telegram Mini App
- другие интерфейсы взаимодействия с пользователем.

Непосредственное взаимодействие с API YooKassa выполняется только серверной частью системы Kircraft (Backend API). Клиентские интерфейсы не взаимодействуют с YooKassa напрямую.

В рамках интеграции используется REST API YooKassa.

Основной сценарий интеграции включает:

1. Создание платежа через метод POST /payments.
2. Получение объекта платежа и ссылки на страницу оплаты (confirmation_url).
3. Перенаправление пользователя на страницу оплаты YooKassa.
4. Выполнение пользователем платежа.
5. Возврат пользователя на страницу системы Kircraft.
6. Получение системой статуса платежа через уведомления YooKassa или запрос GET /payments/{payment_id}.

После успешного завершения платежа система Kircraft фиксирует результат оплаты и переводит заказ клиента в соответствующий статус.

Интеграция реализуется по серверной схеме:

Клиентский интерфейс → Backend API Kircraft → YooKassa API → страница оплаты YooKassa → Backend API Kircraft → Клиентский интерфейс

Это обеспечивает безопасность обработки платежей, так как учетные данные интеграции и секретные ключи YooKassa хранятся только на серверной стороне системы.
(Кусок архитектуры) - вставить скрин
___

## 2. Авторизация при работе с YooKassa API

API YooKassa использует серверную аутентификацию на основе механизма **HTTP Basic Auth**.

Каждый запрос к API должен содержать учетные данные магазина:

- `shopId` — идентификатор магазина  
- `secretKey` — секретный ключ API

Формат передачи авторизации:

Authorization: Basic base64(:)

Все обращения к API YooKassa выполняются только со стороны **Backend API Kircraft**.  
Клиентские интерфейсы (Telegram Bot, Web Interface, Telegram Mini App) не взаимодействуют с API YooKassa напрямую и не имеют доступа к учетным данным интеграции.

### Конфигурация Backend API

В конфигурации Backend API должны храниться следующие параметры:

- `YOOKASSA_SHOP_ID`
- `YOOKASSA_SECRET_KEY`
- `YOOKASSA_API_BASE_URL`

Пример базового URL:

https://api.yookassa.ru/v3

Хранение конфигурации определяется архитектурой Backend API и может быть реализовано через переменные окружения, конфигурационный файл или иное защищенное хранилище.

### Дополнительные требования

При выполнении операции создания платежа (`POST /payments`) должен передаваться заголовок:

Idempotence-Key

Заголовок используется для защиты от повторного создания одного и того же платежа при повторной отправке запроса.


---

## 3. API-документация внешней системы

В рамках интеграции система Kircraft использует REST API платежного сервиса YooKassa.

Документация YooKassa для разработчиков:
https://yookassa.ru/developers/api

Интеграция выполняется через HTTPS-запросы к API YooKassa.

Базовый URL API:

https://api.yookassa.ru/v3

В рамках текущего сценария интеграции используются следующие методы API.

Используемые методы API YooKassa

POST /payments  
Создание платежа.

Метод используется системой Kircraft для создания нового платежа при оформлении заказа клиентом.

В ответе возвращается объект платежа, содержащий:

- идентификатор платежа (payment_id)
- текущий статус платежа
- объект confirmation
- ссылку confirmation_url для перехода пользователя на страницу оплаты YooKassa.

После получения confirmation_url клиентский интерфейс перенаправляет пользователя на страницу оплаты YooKassa.

GET /payments/{payment_id}  
Получение информации о платеже.

Метод используется системой Kircraft для получения актуального статуса платежа.

Запрос выполняется по идентификатору платежа, полученному при создании платежа.

Ответ содержит текущий статус платежа, например:

- pending
- succeeded
- canceled

Полученная информация используется системой Kircraft для определения результата оплаты и изменения статуса заказа.

Ограничения внешней системы

При работе с API YooKassa необходимо учитывать следующие особенности.

Требования авторизации  
Все запросы к API должны содержать заголовок Authorization с использованием механизма Basic Auth.

Идемпотентность операций  
При создании платежа (POST /payments) должен передаваться заголовок Idempotence-Key для защиты от повторного создания платежа при повторной отправке запроса.

Сетевые ограничения  
При взаимодействии с API возможны сетевые таймауты или временная недоступность сервиса. Система Kircraft должна корректно обрабатывать подобные ситуации.

---

# 3. Ограничения внешней системы

- взаимодействие только через HTTPS
- обязательная авторизация API (ShopID + Secret Key)
- webhook должен быть доступен публично
- идемпотентность запросов при создании платежа
- возможные ограничения по rate limit

---

# 4. Сценарий работы (Use Case)

## Пользователи

- Client (пользователь системы)

## Внутренние системы

- Client Interface (Telegram Bot / Web / Mini App)
- Backend API
- Database

## Внешние системы

- YooKassa API

---

## Входные данные

- order_id
- сумма заказа
- валюта
- описание заказа

---

## Предусловие

- заказ существует в системе
- статус заказа = `CREATED`
- заказ еще не оплачен

---

## Основной алгоритм работы

1. Пользователь нажимает кнопку **Оплатить заказ** в клиентском интерфейсе.
2. Client Interface отправляет запрос в Backend API на создание платежа.
3. Backend API проверяет существование и статус заказа.
4. Backend API создает запись в таблице `payments` со статусом `PENDING`.
5. Backend API отправляет запрос **Create Payment** в YooKassa API.
6. YooKassa возвращает:
   - `payment_id`
   - `confirmation_url`
7. Backend сохраняет `payment_id` в таблице `payments`.
8. Backend возвращает `confirmation_url` клиентскому интерфейсу.
9. Пользователь перенаправляется на страницу оплаты YooKassa.
10. Пользователь выполняет оплату.
11. YooKassa отправляет webhook уведомление о статусе платежа.
12. Backend принимает webhook.
13. Backend проверяет подпись webhook.
14. Backend обновляет статус платежа в таблице `payments`.
15. Backend обновляет статус заказа в таблице `orders`.

---

## Альтернативные сценарии и обработка ошибок

### Ошибка создания платежа

- YooKassa возвращает ошибку
- Backend фиксирует ошибку
- пользователю возвращается сообщение о невозможности создать платеж

### Пользователь не завершил оплату

- платеж остается в статусе `pending`
- заказ остается в статусе `created`

### Ошибка webhook

- webhook логируется
- система пытается повторно обработать событие

---

## Результат

В системе фиксируется:

- статус платежа
- идентификатор платежа YooKassa
- обновленный статус заказа

---

# 5. Диаграмма процесса (UML Sequence)

Участники процесса:

- Client
- Client Interface
- Backend API
- Database
- YooKassa

Диаграмма описывает процесс:

- создание платежа
- редирект пользователя
- webhook уведомление
- обновление статусов

(диаграмма хранится в файле `payment-sequence`)

---

# 6. Используемые API-методы

## Создание платежа

Метод:

POST `/payments`

Назначение:

Создание платежа в системе YooKassa.

---

## Получение информации о платеже

Метод:

GET `/payments/{payment_id}`

Назначение:

Получение статуса платежа.

---

## Webhook уведомления

Endpoint системы:

POST `/api/payments/yookassa/webhook`

Назначение:

Получение уведомлений о статусе платежа.

---

# 7. Маппинг данных

(см. файл `payment-mapping.md`)

---

# 8. Маппинг UI/UX и API

Экран: Оплата заказа

| UI элемент | API |
|---|---|
| кнопка "Оплатить" | POST /payments/create |
| редирект на оплату | confirmation_url |

---

# 9. Логирование

Необходимо логировать:

- создание платежа
- запрос в YooKassa API
- ответ YooKassa
- получение webhook
- изменение статуса платежа
- изменение статуса заказа

---

# 10. Дополнительно

Возможные будущие сценарии:

- возврат платежа (refund)
- повторная попытка оплаты
- отмена платежа
- проверка статуса платежа
